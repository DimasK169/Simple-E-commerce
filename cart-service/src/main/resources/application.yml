spring:
  datasource:
    cart:
      jdbc-url: jdbc:postgresql://localhost:5432/db_cart
      username: postgres
      password: admin
    product:
      jdbc-url: jdbc:postgresql://localhost:5432/db_mst_product
      username: postgres
      password: admin
    flash-sale:
      jdbc-url: jdbc:postgresql://localhost:5432/db_flashsale
      username: postgres
      password: admin
    user:
      jdbc-url: jdbc:postgresql://localhost:5432/db_mst_user
      username: postgres
      password: admin

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update


